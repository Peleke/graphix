# Schemathesis Configuration (v4.12.0+)
#
# Flat top-level keys (no [schemathesis] wrapper).
# Base URL and spec URL are passed via CLI flags in run-fuzz.sh.

[checks]
all = true
response-schema-conformance = true
response-headers-conformance = true
response-content-type-conformance = true
status-code-conformance = true

[generation]
max-examples = 50
deadline = 5000
