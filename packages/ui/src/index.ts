/**
 * @graphix/ui
 *
 * UI components and design system for Graphix.
 *
 * This package provides:
 * - Design tokens and theme system (Panda CSS)
 * - Canvas management (Fabric.js)
 * - UI components (Radix UI + Panda CSS)
 * - State management patterns (Zustand)
 */

// Re-export theme system
export * from "./theme";

// Re-export canvas utilities
export * from "./canvas";

// Package version
export const VERSION = "0.1.0";
